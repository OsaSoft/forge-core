
When presenting visual or design choices (icon styles, layouts, color schemes, component variants), use AskUserQuestion with the `preview` field on each option.

Preview content: realistic ASCII mockup showing the option in context. Include both default and active/toggled states. The user must be able to compare options visually, not from descriptions.