Everything is markdown. Skills, agents, rules, ADRs, journal templates, documentation — all authored as markdown with YAML frontmatter. No code templates, JSON definitions, or proprietary plugin formats.

YAML frontmatter carries machine-readable metadata. The prose body carries instructions. The same file works in a git repo, an Obsidian vault, and an AI tool's context window without conversion.
