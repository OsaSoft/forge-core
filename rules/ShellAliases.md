---
targets: [claude, codex]
---
For commands RTK does not cover, use `command` prefix to bypass aliases:
- `command cd DIR` (not `cd DIR`)
- `command cp SRC DST` (not `cp SRC DST`)
- `command mv SRC DST` (not `mv SRC DST`)
- `command rm FILE` (not `rm FILE`)
