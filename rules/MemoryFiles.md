Memory (in Claude Code) loads only the first 200 lines at session start. Content beyond is silently ignored. 
Avoid using externally managed memory files like MEMORY.md.
Move detailed notes into separate topic files read on demand.
