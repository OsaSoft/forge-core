# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Module-building skills for the forge ecosystem. Teaches AI coding tools to create skills, scaffold agents, and architect modules.

## Skills

| Skill                    | What it does                                                                         |
| ------------------------ | ------------------------------------------------------------------------------------ |
| **BuildSkill**           | Create and validate skill definitions (SKILL.md structure, frontmatter, conventions) |
| **BuildAgent**           | Scaffold, validate, and audit agent markdown files (frontmatter, body, deployment)   |
| **BuildModule**          | Design and validate forge modules (directory layout, config, three-layer arch)        |
| **BuildHook**            | Create and validate module hooks (event handling, hook scripts, platform wiring)      |
| **BuildPlugin**          | Create, validate, and publish Claude Code plugins from forge modules                 |
| **ArchitectureDecision** | Find, create, validate, and capture ADRs with forge-adr schema validation            |
| **VersionControl**       | Git best practices, conventional commits, staging, push policy, repo governance      |
| **MarkdownLint**         | Format and lint markdown documents (backtick refs, bare URLs, list formatting)        |
| **MarkdownSchema**       | Create, derive, and validate .mdschema files for structured markdown                 |
| **SettingsMaintenance**  | Audit and clean AI coding tool settings (permissions, plugins, hooks, conflicts)      |
| **SystemCheck**          | Check forge ecosystem for staleness (skills, binaries, lib consistency, version drift)|
| **AdaptPrompts**         | Adapt generic rules and prompts for independent repos (strip branding, scope paths)   |
| **PublishPrompts**       | Provenance tracking and sync for inherited rules, skills, and agents                 |
| **PromptAnalysis**       | Validate and minimize prompts (provenance, targeting, staleness, redundancy)          |
| **RTK**                  | RTK (Rust Token Killer) token-optimized CLI proxy setup and reference                |
| **HtmlPlayground**       | Generate single-file HTML demos comparing techniques side-by-side                    |

## Build & Test

```sh
make install          # deploy skills + activate git pre-commit hooks (requires forge-cli)
make test             # forge validate + ADR schema self-test + frontmatter validation
make lint             # shellcheck (CI also runs ruff + tsc)
make check            # verify prerequisites and module structure
make verify           # forge validate only
make clean            # remove build artifacts
```

Validate a single ADR: `python3 bin/validate-adr.py templates/forge-adr.json "docs/decisions/CORE-0001 Markdown as System Language.md"`

Run validator self-tests: `python3 bin/validate-adr.py --test`

## Architecture

```
skills/                   Skill definitions deployed to all providers
  ArchitectureDecision/   SKILL.md + companions + user/ overlay
  BuildSkill/             SKILL.md + SKILL.yaml + ClaudeSkill.md
  */                      SKILL.md + SKILL.yaml + optional companions
docs/decisions/           ADRs with prefix sections (CORE-, ARCH-, PROV-, MVPR-)
templates/                ADR templates + JSON schemas
bin/                      validate-adr.py (ADR frontmatter validator)
rules/                    Behavioral rules deployed to .claude/rules/
.githooks/                Git pre-commit hook (ADR, shell, rust, python, typescript, gitleaks)
.github/workflows/        CI pipeline (ADR validation, shellcheck, ruff, tsc)
defaults.yaml             Skill roster + ADR config
config.yaml               User overrides (gitignored)
module.yaml               Module metadata (name, version)
```

Platform directories (`.claude/`, `.gemini/`, `.codex/`, `.opencode/`) are generated by `make install` and gitignored.

### Skill companion pattern

Skills use `@` includes to reference companion files. Forge-specific companions live in `user/` subdirectories — these can be conditionally loaded in downstream repos:

```
skills/ArchitectureDecision/
    SKILL.md                    Core skill (@ references all companions)
    TemplateReference.md        Template tiers and frontmatter spec
    SchemaValidation.md         Validation tools and commands
    user/
        ForgeADR.md             Forge-specific template choice
        ContextKeeper.md        c0ntextkeeper MCP integration
```

### ADR template tiers

| Template                       | Schema                           | When to use                         |
| ------------------------------ | -------------------------------- | ----------------------------------- |
| `templates/adr.md`             | —                                | Minimal Nygard records              |
| `templates/madr.md`            | —                                | MADR light for standard decisions   |
| `templates/forge-adr.md`       | `templates/forge-adr.json`       | Forge ecosystem ADRs (default)      |
| `templates/structured-madr.md` | `templates/structured-madr.json` | Upstream-compatible shared repos    |

Templates use `${VARIABLE}` placeholders resolvable by `envsubst`, with `%%` comments for inline guidance.

## ADR Prefix Sections

| Prefix | Scope                    | Example                                      |
| ------ | ------------------------ | -------------------------------------------- |
| `CORE` | Markdown and scaffolding | `CORE-0001 Markdown as System Language`      |
| `ARCH` | Ecosystem architecture   | `ARCH-0001 Skills Agents and Rules`          |
| `PROV` | Manifest and provenance  | `PROV-0002 Manifest for Deployment Tracking` |
| `MVPR` | Prompt optimization      | `MVPR-0001 Minimum Viable Prompt`            |

Each prefix numbers independently. ADRs use [structured-madr](https://github.com/zircote/structured-madr) frontmatter with forge extensions (CORE-0007). No forward references — a lower-numbered ADR must not reference a higher-numbered one.

## Conventions

### Configuration

- `defaults.yaml`: canonical skill roster + ADR config — edit when adding/removing skills
- `config.yaml`: user overrides (gitignored) — same structure, only changed fields
- `module.yaml`: module metadata — update `version` on releases

### forge-cli

External binary (`forge`) providing install, validate, and assembly operations. Required for `make install` and `make test`.

### Git

Conventional Commits: `type: description`. Lowercase, no trailing period, no scope. Types: `feat`, `fix`, `docs`, `chore`, `refactor`, `test`.
